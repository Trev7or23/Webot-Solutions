---
import { Icon } from "astro-icon/components";

interface Props {
    logo: string;
}
const { logo } = Astro.props;
---

<div class="flex items-center text-white font-bold text-xl">
    <Icon name={logo} class="size-6" />
    <slot />
</div>
